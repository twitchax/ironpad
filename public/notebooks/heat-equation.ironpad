{
  "cells": [
    {
      "id": "cell_1",
      "source": "/// # 2D Heat Equation Simulation\n///\n/// Solves the 2D heat equation  **\u2202u/\u2202t = \u03b1 \u2207\u00b2u**  on a 200\u00d7200 grid\n/// using explicit finite differences.  Several hot and cold spots seed\n/// the initial temperature field; watch them diffuse over time.\n\nconst W: u32 = 400;\nconst H: u32 = 400;\nconst N: usize = W as usize;\nconst M: usize = H as usize;\nconst ALPHA: f64 = 0.25;\n\nstruct HeatEquation {\n    u: Vec<f64>,\n    buf: Vec<f64>,\n    step: u32,\n}\n\nimpl HeatEquation {\n    fn idx(x: usize, y: usize) -> usize {\n        y * N + x\n    }\n\n    fn temp_to_color(t: f64) -> (u8, u8, u8) {\n        // Cool (dark blue) \u2192 warm (orange/yellow) \u2192 hot (white).\n        let t = t.clamp(0.0, 1.0);\n        if t < 0.25 {\n            let s = t / 0.25;\n            let r = (10.0 + s * 20.0) as u8;\n            let g = (10.0 + s * 10.0) as u8;\n            let b = (40.0 + s * 160.0) as u8;\n            (r, g, b)\n        } else if t < 0.5 {\n            let s = (t - 0.25) / 0.25;\n            let r = (30.0 + s * 200.0) as u8;\n            let g = (20.0 + s * 60.0) as u8;\n            let b = (200.0 - s * 100.0) as u8;\n            (r, g, b)\n        } else if t < 0.75 {\n            let s = (t - 0.5) / 0.25;\n            let r = (230.0 + s * 25.0) as u8;\n            let g = (80.0 + s * 140.0) as u8;\n            let b = (100.0 - s * 80.0) as u8;\n            (r, g, b)\n        } else {\n            let s = (t - 0.75) / 0.25;\n            let r = 255;\n            let g = (220.0 + s * 35.0) as u8;\n            let b = (20.0 + s * 235.0) as u8;\n            (r, g, b)\n        }\n    }\n\n    fn set_circle(u: &mut [f64], cx: usize, cy: usize, r: usize, temp: f64) {\n        let r2 = (r * r) as f64;\n        let x_min = cx.saturating_sub(r);\n        let x_max = (cx + r).min(N - 1);\n        let y_min = cy.saturating_sub(r);\n        let y_max = (cy + r).min(M - 1);\n        for y in y_min..=y_max {\n            for x in x_min..=x_max {\n                let dx = x as f64 - cx as f64;\n                let dy = y as f64 - cy as f64;\n                if dx * dx + dy * dy <= r2 {\n                    u[Self::idx(x, y)] = temp;\n                }\n            }\n        }\n    }\n}\n\nimpl Simulation for HeatEquation {\n    fn init() -> Self {\n        let mut u = vec![0.0; N * M];\n\n        // Hot spots.\n        Self::set_circle(&mut u, N / 4, M / 4, 20, 1.0);\n        Self::set_circle(&mut u, 3 * N / 4, M / 4, 15, 0.9);\n        Self::set_circle(&mut u, N / 2, 3 * M / 4, 25, 1.0);\n\n        // Cool spots.\n        Self::set_circle(&mut u, 3 * N / 4, 3 * M / 4, 18, 0.0);\n        Self::set_circle(&mut u, N / 4, 3 * M / 4, 12, 0.0);\n\n        let buf = u.clone();\n        Self { u, buf, step: 0 }\n    }\n\n    fn tick(&mut self) -> Canvas {\n        // Run several diffusion sub-steps per frame for visible evolution.\n        for _ in 0..10 {\n            for y in 1..M - 1 {\n                for x in 1..N - 1 {\n                    let c = Self::idx(x, y);\n                    let laplacian = self.u[Self::idx(x + 1, y)]\n                        + self.u[Self::idx(x - 1, y)]\n                        + self.u[Self::idx(x, y + 1)]\n                        + self.u[Self::idx(x, y - 1)]\n                        - 4.0 * self.u[c];\n                    self.buf[c] = self.u[c] + ALPHA * laplacian;\n                }\n            }\n            std::mem::swap(&mut self.u, &mut self.buf);\n        }\n\n        self.step += 1;\n\n        Canvas::from_fn(W, H, |x, y| {\n            let t = self.u[Self::idx(x as usize, y as usize)];\n            Self::temp_to_color(t)\n        })\n    }\n\n    fn fps() -> u32 {\n        30\n    }\n}",
      "cargo_toml": null,
      "order": 0,
      "label": "Heat Diffusion",
      "cell_type": "Code"
    }
  ],
  "version": 1
}
